<tr>
  <td>
    <b><%= account.name %></b> - <%= account.currency.name %>
  </td>
  <td class="td_numero">
    <%= account.saldo %>
  </td>
  <td style="width: 1%">
    <%= check_box_tag "account_#{account.id}" , account.id, chequeado?("account", account.id) if @side_con_form %>
  </td>
</tr>